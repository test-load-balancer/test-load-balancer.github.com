<strong>if</strong> on 'balancer side' and 'balancing against TLB server', which means
<code class="block">((using java-framework balancing support) OR ($TLB_APP == 'tlb.balancer.BalancerInitializer')) AND ($TYPE_OF_SERVER == 'tlb.service.TlbServer')</code>
<hr/>
