<strong>if</strong> on 'balancer side' and 'balancing against Go server', which means
<code class="block">((using java-framework balancing support) OR ($TLB_APP == 'tlb.balancer.BalancerInitializer')) AND ($TALK_TO_SERVICE == 'tlb.service.TalkToGoServer')</code>
<hr/>
